const data_dir = './data'
const completed_bulk_transfer_dir = `${data_dir}/results`
const scan_configs_dir = `${data_dir}/scan-configs`

module.exports = {
    completed_bulk_transfer_dir,
    data_dir,
    scan_configs_dir
}
